import React from 'react';
import { params } from './gateStyles';
import { BaseSpacer } from '@cpmech/rncomps';

export const GateVSpaceLarge: React.FC = () => {
  return <BaseSpacer space={params.vspace.large} />;
};
